<h1>sub1</h1>
<p>공지사항 리스트</p>
<table>
  <tr>
    <th>번호</th>
    <th>내용</th>
    <th>작성자</th>
    <th>수정</th>
  </tr>
  <% rows1.forEach((row, index)=>{ %>
  <tr>
    <td><%= row.id %></td>
    <td onclick="javascript:location.href='/content?id=<%= row.id %>'"><%= row.content %></td>
    <td><%= row.name %></td>
    <td>
      <input type="button" value="수정" onclick="javascript:location.href='/updateM?id=<%= row.id %>'">
      <input type="button" value="삭제" onclick="javascript:location.href='/deleteM?id=<%= row.id %>'">
    </td>
  </tr>
  <%  }); %>
  <% rows2.forEach((row, index)=>{ %>
  <tr>
    <td><%= row.id %></td>
    <td onclick="javascript:location.href='/content?id=<%= row.id %>'"><%= row.content %></td>
    <td><%= row.name %></td>
    <td>
      <input type="button" value="수정" onclick="javascript:location.href='/updateM?id=<%= row.id %>'">
      <input type="button" value="삭제" onclick="javascript:location.href='/deleteM?id=<%= row.id %>'">
    </td>
  </tr>
  <%  }); %>
</table>